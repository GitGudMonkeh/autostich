# Öffentliche Supabase-Config für lokales `npm run dev` (siehe .env.production).
# Kein Secret — RLS schützt; nur select + insert erlaubt (#14). Eigenes, dediziertes
# Autostich-Projekt (nicht mehr mit Prototyp 1/TrickLadder geteilt), Tabelle `autostich_scores`.
VITE_SUPABASE_URL=https://uhuwgzhllnigcucmhclf.supabase.co
VITE_SUPABASE_KEY=sb_publishable_XwaG0xtiD4Dkg58SkxaPNQ_PUMhdgAG

# #F-01: Musik/Medien liegen außerhalb des Vite-Graphen (media/ im Repo). Im Dev-Server liefert die
# Middleware in vite.config.js sie unter /media/ aus. Produktion nutzt den absoluten Pages-Pfad.
VITE_MEDIA_BASE=/media/
