# Handoff — M6 · the glossary

1. **Done.** Migration only, as the contract requires. Five commits on `task/menu-m6-glossary` (base `db7403bf`): `297436d5` the mount harness · `5e3c9755` the migration · `ac752bfc` the guards and eighteen counter-checks · `2be6ea77` record, evidence and owner set · this handoff. Tree clean, worktree left in place, **no merge, no push, no PR**.
2. **Did the vocabulary hold? Yes — nine literals resolved to steps and nothing was coined.** `.gl-dim`'s wash **is** `--sf-scrim-desk`, `.gl-head`'s fade **is** `--sf-head-fade`, both panel gradients **are** `--sf-glass`, both panel radii **are** `--rd-lg`, `.gl-ticon`'s 8 **is** `--rd-md`, and the two inline `#2c2a3a` **are** `--ed-base` — character for character in every case. Three more are the translucent neutral edge, pulled onto `--ed-quiet`. **One `!important` fell** (`.gl-head` re-points the step on the element instead of shouting at its own inline style — §2c's sanctioned form, `.gd-head`'s verbatim); the two that stay each override a **different** inline literal, named at the site.
3. **The five surfaces at zero deltas: YES, all five, printed.** `skill-choice`, `perk-choice`, `run-stage`, `architect` and `hub` return **zero lines** from the delta file. 160 cells, 25 189 matched nodes, 0 unmatched; the *by surface* column of the distribution contains exactly one word — `glossary`, 1110 deltas, **all `bc`**, exactly **111 per cell in all ten** (109 term cards + the search field + the note under the column). No surface, radius, elevation, inset or box moved anywhere in the tree.
4. **The uncovered mount I measured, and what came out.** The survey's `glossary` cell opens from **`CornerTools`**, not the mainscreen — measured, not assumed: two glossary buttons are visible on the hub above 1280 px and the survey takes `vis[0]`, which is DOM 0. So I opened it from **`SkillSelect`, the pick phase**, through the survey's own step list: **26 of 26 changed selectors present in all four cells**, 1796 matched nodes, 0 unmatched, and **444 deltas that are the same 111 `bc` per cell** as the survey's own. The overlay draws the same thing from inside the battle session as from the hub, and the pick screen itself did not move. `.gloss-term` counts 12 there, so **`GlossaryText` — the export with no overlay of its own — is measured too**, not argued about.
5. **The `hub` clause is the one I would flag to the planner.** It cost nothing to get right and would have cost a workstream to get wrong: `StartScreen`'s footer button is a live mount that the matrix does **not** open, so a leak into it would have reached the mainscreen's baseline before its first measurement. Zero at all ten cells.
6. **Overflow held against 157–170: measured 148–171, and the difference is a finding (M6-F08).** Taken **before** the diff and identical **cell by cell** after it, 0 truncated throughout. The wide viewports read nine lower than the recorded band and 1280 reads one higher. The instrument is not drifting — the end screen still reads M4's **147** on the same run. The successor inherits a per-cell table rather than a range that matches nothing.
7. **Thirteen declarations painted nothing and are gone (M6-F01).** `#gl-ruhe` stood a second block beside the first instead of editing it — `conventions.md` §1 rule 2 by name — so `.gl-navrow`, `.gl-navdot`, `.gl-navcount` and the term card each lost a fill, an edge, a radius or a padding to a later rule of equal specificity. **Which of them paint is measured, not read:** every one was pulled back off the live overlay as a computed value. Deleted rather than tokenised — M5's answer at `.gd-navrow` — and **the gate is the proof they were dead**.
8. **Tripwires: none fired.** No new value at a call site, no own panel, no minted token, no `@theme` edit, no `.as-edge-*` alpha, no `--text-*`, nothing below 1280 px moved, `typo-tokens.test.js` unmodified.
9. **Guards: measured which break, and the answer is none.** `glossary-desktop.test.js` — the contract's likely candidate — stayed green, all eleven. `panel-tokens.test.js` stayed green because this file was not yet on its allowlist. And **the MENU-38 liveness probe stayed green, which is M5's repair paying out**: it was a floor of 30 on a shrinking count, and three more conversions here would have taken it under again.
10. **So the guard work is the ratchet closing, plus one boundary and one new seam.** The allowlist entry is `/\.gl-(?!wrap)/`, because **`.gl-` is not the glossary** (M6-F09): `.lb-pagescroll:has(.lb-cockpit) > .gl-wrap` is `GlobalLeaderboard.jsx`. That is `.up-banner` and MENU-38 a third time. The rule carries **no literal at all**, so no axis check and no ratchet could ever have caught the mistake — hence its own counter-check, in H-e's shape: `.gl-wrap` must still exist, and no rule coming in through M6's prefixes may carry it.
11. **The fifth silent seam.** The head's fill now arrives through the variable the inline style reads, and that breaks silently two ways: `STICKY_HEAD_BG` going back to a literal, or the re-point being deleted. The guard **computes** — local custom properties substituted into the rule's `background`, compared to the resolved `--sf-head-fade` — **with a negative probe** that it must not equal the resolved `--sf-head`, because without it the check passes on a tree where the re-point is gone and both sides read the same step.
12. **Eighteen counter-checks, run and restored in a `finally`, 0 unexpected** (`evidence/M6/counter-checks.mjs`, output in `counter-checks.txt`): twelve red-on-defect, **two green-on-work**, two liveness, both clean baselines. The green pair is the direction that matters — a further correct migration elsewhere lowers the family count and nothing goes red, and a future tidy-up of `#gl-ruhe`'s remaining leftovers stays green.
13. **Gates, run bare without pipes:** `npm test` **exit 0** (143 files / **2306** tests) · `npm run lint -- --max-warnings=0` **exit 0** · `npm run build` **exit 0** · `npm run gen:db` **exit 0**. 2306 rather than 2300 = four ratchet entries, the `.gl-wrap` boundary check and the fifth seam. Ratchets: **MENU-38 at 0 on both M6 units** (three converted, one deleted as dead — stated in both halves), ink 8 in `Glossary.jsx` and 14 in the rules.
14. **Owner set:** `evidence/M6/owner/` — four PNGs, both languages at 1280×720 and 1920×1080, captured **from inside the pick phase** because that is the mount that needed showing. **One was opened and confirmed to be an image.** A visual result is not reported as approved.
15. **What the next task inherits.** (a) **M6-F01's scope is the only owner question** — five more declarations in those same rules are equally dead but are not on the five axes, so tidying them is not migration; the counter-check proves the successor can. (b) **M6-F02**: six of nine mounts still have no cell; `mounts.mjs` needs one step list each. (c) **M6-F03** — `#2a2a33` stands at one site, the value the planner already granted for M5, untaken here only because `#gl-ruhe` set that border's width to 0 and it paints nothing. (d) **M6-F06**: `--rd-md` is labelled *"a control that is not a tile"* and has now landed on an icon tile twice (M8's `.lb-ctxicon`, this task's `.gl-ticon`); a third sighting is worth the rung's wording. (e) **M6-F10**: `.as-chip`/`.as-chiprow` belong to no migrated unit and carry two MENU-38 alphas plus a surface — two more members of the twelve, outside every ratchet, for whoever migrates `.as-edge-*`. (f) **M6-F07**: state which side of the `var()` fallback line a colour count is taken on, or it cannot be reproduced.
