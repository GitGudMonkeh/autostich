# Handoff — M2b · the workshop's contents

1. **Done.** The workshop's contents take the panel vocabulary; 29 of the file's 35 menu-side axis
   literals now name a step. `task/menu-m2b-workshop-contents`, four commits on `4d16a5da`:
   `cab97a10` migration + guard · `631388a4` guard fix · `d2b2143e` record + evidence · `1ef21c13`
   owner verdict. Four gates green; `typo-tokens.test.js` unmodified.
2. **Did the vocabulary hold? Partly — and the six misses are findings, not requests.** Three
   state-colour pairs have no step for their role (MENU-46 accent-tinted, MENU-47 unlock,
   **MENU-48** affirmative — `--ctl-danger`/`--ctl-danger-wash` have no green counterpart, measured
   15/15/7 from the transposed recipe), plus a selection ring (MENU-50) and a chip's label padding
   (MENU-51). Nothing was minted and nothing was worked around at a call site.
3. **The allowlist covers the whole file. `hooks` is gone, not widened** — zero entries remain. The 32
   surviving literals draw the simulated board, and the fifteen scene components are named
   individually as permanently outside the axes (owner-approved, Q2). Ink holds at **27** for the
   file; new CSS entry `.cz-*` contents = **1**. M2a's shell stays at 2 on its own selector list.
4. **Tripwire 1: not triggered** — no new background, padding, radius or shadow value at any call
   site. **Tripwire 2: not triggered** — no panel built.
5. **Guards: measured, not inferred (H-a). `cz-ruhe`, `shop-scale` and `fx-panel` pass UNMODIFIED** —
   M1 and M2a had already rewritten them to invariants. Only `panel-tokens.test.js` changed; **eight
   seams counter-checked** by breaking each and proving it fires, plus three new list-rot checks.
6. **Findings.** Fixed here: MENU-49, MENU-52 (H-b's fifth instance — the inline reader asked whether
   a `var()` appeared, not whether a literal stood beside one; it hid 3 sites), MENU-54, MENU-57.
   Expected: MENU-45, MENU-53, **MENU-58** (harness noise floor measured at **zero** — same tree twice,
   0 deltas over 160 cells). Planner: MENU-46/47/48/50/51, MENU-56. Out of scope: MENU-44 (MENU-38's
   ratchet was ruled at the freeze and never built; this region needed it zero times, and the family
   has an **eighth** alpha), MENU-55.
7. **M3 inherits** a clean tree, the shared worktree `C:/Code/Autostich-worktrees/menu-rework` left in
   place, preview port **5189**, survey port **5181** free. Nothing merged, nothing pushed, no PR.

> **Two for the planner before M3.** (a) **MENU-56** — no capture renders the segmented controls, so
> the largest change the owner approved contributes zero of the 410 deltas; the survey needs a state
> axis, not just a surface axis. (b) **MENU-55** — `surface-delta.mjs` prints its first 200 deltas and
> this task had 410; read from the printed list the evidence looked German-only with a hole at
> 1920×1080. The next task over 200 deltas will read a sample as a census.
