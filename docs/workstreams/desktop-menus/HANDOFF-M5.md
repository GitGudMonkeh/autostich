# Handoff — M5 · the guide

1. **Done.** Migration only, as the contract requires. Five commits on `task/menu-m5-guide` (base `d5f8aece`): `b83e9915` the mount harness · `1c12a452` the migration · `19da60c8` the guard rewrite · `a23e3ac1` record, evidence and owner set · this handoff. Tree clean, worktree left in place, **no merge, no push, no PR**.
2. **Did the vocabulary hold? Yes — all seven `.gd-*` literals resolved to steps.** `.gd-dim`'s wash **is** `--sf-scrim-desk`, `.gd-head`'s fade **is** `--sf-head-fade`, both panel gradients **are** `--sf-glass`, character for character; the other three are the translucent neutral edge, pulled onto `--ed-quiet`. Nothing coined, no tripwire fired. The contract also listed `#82828f` among the seven — measured, it is **ink**, not one of the five axes (M5-F08); the count holds, that category does not.
3. **`skill-choice` at zero deltas: YES.** It does not appear once in the delta list. 160 cells, 25 189 matched nodes, 0 unmatched; **every surface but `guide` at zero**. On `guide`: 60 deltas, all `bc`, exactly six per cell — every one the MENU-38 → `--ed-quiet` conversion the owner accepted at ~9/255 and that M3/M7/M8/M4 pulled 14/4/6/5 times. **The pick phase inherited nothing.**
4. **What `GuideBody` would have touched below 1280 px — measured, not argued.** `DeckDetail` renders it and M3 measured that phone-only. Exactly **one** inline value of the file is a step: `#141419` **is** `--sf-ground`, at three sites, all inside the phone-reachable subtree. The token has **one declaration, no media override**, and computes to `#141419` at 1920, 1280, **1100** (M3's reachable width) and **390**. One value at every width, so the substitution cannot move the phone. Everything not exactly a step stayed a literal.
5. **The one thing I did not take:** `#2a2a33` stands at **eight** sites and misses `--ed-quiet` (`#2a2a34`) by **one unit of blue** — the file's whole neutral-edge vocabulary, one short of the ladder's. That is *below* the 1.8/255 M3 was granted, but M3's grant created **no standing permission**, M4 asked for a larger one and was **refused**, and these eight are read on the phone. **Asked, not taken (M5-F02).**
6. **Changed sites inside a measured cell: yes, and in two cells.** The survey's `guide` covers the tree mount. `evidence/M5/mounts.mjs` opens the overlay **from inside the pick phase** — the mount that leaves this round — and censuses **20 of 20** changed selectors present in all four cells (both languages, 1280×720 and 1920×1080), with the **same six `bc` deltas per cell** as the tree mount. The third mount (`GameOver.jsx:426`) has no cell and is reported as such, not waved away (M5-F06).
7. **Two declarations painted nothing and are gone (M5-F01):** `.gd-close`'s `border-radius: 8px` and `.gd-navrow`'s `11px`, both overridden by the `#eckig` collector rules at equal specificity and later in the sheet. Deleted rather than tokenised — M3's answer at `.up-navrow`. **Proof they were dead: zero radius deltas anywhere.**
8. **Tripwires: none fired.** No new value at a call site, no own panel, no minted token, no `@theme` edit, no `.as-edge-*` alpha, nothing below 1280 px moved, `typo-tokens.test.js` unmodified.
9. **Guards: the contract's prediction was wrong, and the one that broke was shared (M5-F07).** `guide-desktop.test.js` stayed green, all ten. The **MENU-38 liveness probe** went red — and it went red **because the migration succeeded**: it was `edgeIn(whole tree) > 30`, a floor under a number that falls every time a worker does the right thing (58 when written, 30 as the floor, 29 after my three conversions). That is MR1's H-c word for word.
10. **Rewritten to the invariant, not to a lower number.** The invariant is *the expression recognises the family*, which depends on no migration state: a **positive control** on a fixed string, a **negative control** on a neighbour it must not take (H-e's shape — *contains no X other than Y*), and an **anchor** on the three `.as-edge-*` declarations, a region every contract of this round names as a non-goal, asserted with `toBe(3)` rather than a floor.
11. **Counter-checked eight ways, and the decisive case is the last:** simulating a *further* successful migration takes the tree to **16** — far below the old floor — and the rewritten probe stays **green**, where the old one would have been red. The seven defects it exists for all still turn it red.
12. **My own harness lied once, and the fix is the same shape (M5-F05).** "The only visible button whose text is `i`" also matches the hub's **glossary** button, still in the document behind the run and earlier in DOM order: the first run opened the glossary, reported `ok`, and failed four cells later on a marker that could never appear. Now written as *the visible `i` that is not `.gloss-i-btn`*, with its 18-px box asserted rather than assumed.
13. **Gates, run bare without pipes:** `npm test` **exit 0** (143 files / **2300** tests) · `npm run lint -- --max-warnings=0` **exit 0** · `npm run build` **exit 0** · `npm run gen:db` **exit 0**. 2300 rather than 2296 = M5's two ink and two edge ratchet entries.
14. **Owner set:** `evidence/M5/owner/` — four PNGs, both languages at both binding viewports, captured **from inside the pick phase** because that is the mount that needed showing. **One was opened and confirmed to be an image.** A visual result is not reported as approved.
15. **What the next task inherits.** (a) **M5-F02** is open and is the only owner question. (b) `mounts.mjs` needs only a second entry to close **M5-F06** (the `GameOver` mount). (c) **M5-F04** — the guide's navigation row keeps the accent gradient M3 deleted from the tree's, because nothing here overrides it; removing it is a composition change and needs a design. (d) The liveness probe's rewrite is reusable: **any ratchet whose liveness is a floor on a shrinking count will break the same way.**
