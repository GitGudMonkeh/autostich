# Handoff — MR1 · the row ground

1. **Done.** `--sf-row` is declared once in `@theme` and all eight sites read it. Four commits on `task/menu-mr1-row-ground` (base `64f304a5`): `8ecc59f9` the two harnesses · `ce368c16` the migration and the guard · `eb2d83d1` record and evidence · this handoff. Tree clean, worktree left in place, **no merge, no push, no PR**. Gates: 143 files / **2289** tests · lint · build · gen:db, all exit 0 — 2289 rather than 2287 because the new guard is two tests.
2. **Zero computed deltas on every surface: yes.** 200 cells, 28 619 matched nodes, 0 unmatched paths, **0 deltas**, across three before/after pairs. The noise floor is zero and was *measured* in this task, not inherited: two independent runs of the same build are identical node for node.
3. **Eight sites found — exactly the contract's list, line for line, and no ninth.** The two JS constants have five inline readers between them, so eight declarations paint thirteen places. One near-miss named and excluded: `rgba(27, 26, 36, 0.72)` on `.as-glass` shares the alpha and is a different colour.
4. **Two of the four `!important` fell:** `.un-first .un-prev` (the contract's case exactly) and `.fb-run` — whose element has **no inline background at all**, so it never had the stated reason (MR1-F03). **Two are load-bearing and stay:** `.gl-search input` and `.lb-mod` override a *different* inline literal, `#0f0f14` and `#17161f` — the desktop re-pointing a phone value, the `--sf-scrim` / `--sf-scrim-desk` shape, not a rule shouting at the row ground. Counter-checked by removal: **31 deltas**, every one a background falling back to the inline literal. Kept with the file, the line and the value named at the site (MR1-F02).
5. **Guards: exactly one broke**, measured before anything was touched — the `stateLiteral` ratchet in both modals. Struck (the ratchet turns down), together with **five surface exemptions that now exempt nothing** and would have let the next literal through in silence. Replaced by the invariant, not a lower number: *the value is written out once, in its own declaration, and every consumer reads the token* — tree-wide, both alpha spellings, JSX read through the allowlist rather than a named pair, plus the pruning half stated as "at least one consumer" and never as a count. Counter-checked four ways, red every time, green on the clean tree. `typo-tokens.test.js` unmodified; no ratchet grew.
6. **Findings:** MR1-F01 … MR1-F07, in `measurements/MR1.md`.
7. **What the next task inherits.** (a) **MR1-F04, the one worth acting on:** the survey opens hub tile 2 — the *board* entry on its default tab — so `.lb-weekcount`, `.lb-ctxtile` and `.lb-mod` are in **no survey cell at all**, and first start is in none either. Covered here by `evidence/MR1/{lb-week,erststart}.mjs`, task-locally; folding either into `viewport-survey.mjs` is a change to shared evidence tooling and was not MR1's to make. (b) The two inline literals behind the surviving `!important` — `Glossary.jsx:169` and `LeaderboardScreen.jsx:113`. Point those at the token and both declarations retire; both files are outside this task's permitted surface. (c) `--sf-row` is vocabulary now: a ninth sighting is a `var()`, and the guard fails anything else.
