# Handoff — M4 · the end screen

1. **Done.** Migration only, as the contract requires: the vocabulary taken, nothing else touched — no composition, no type, no DP price colour, and none of the four foreign components. Five commits on `task/menu-m4-endscreen` (base `654e7bef`): `722db1c5` the coverage harness · `c73cbdfe` the migration · `1e2f62cf` the guards · `43bfa089` record, evidence and owner set · this handoff. Tree clean, worktree left in place, **no merge, no push, no PR**.
2. **Did the vocabulary hold? Yes, with two named differences and no gaps.** Radius, surface, elevation and edge all came from §2c. Nothing was coined, nothing was worked around at the call site, and no tripwire fired. What did not fit is **counted and reported**, never moved to make a guard green.
3. **Every surface but `victory` at zero.** 160 cells, 25 189 matched nodes, 0 unmatched. On `victory`: **79 deltas, all of them `bc`**, every one `rgba(150, 150, 170, .12)` → `rgb(42, 42, 52)` — MENU-38 → `--ed-quiet`, the conversion M2a measured at ~9/255 and the owner accepted, and that M3 pulled 14×, M7 4× and M8 6×. No surface, radius, inset, elevation or box moved anywhere.
4. **Are the changed sites in a measured cell?** Counted, not assumed: **17 of 22** censused selectors are present, both languages, both binding viewports. **One changed site is in no cell** — `.go-blist` needs an architect building actually *placed*, which the survey's walk never does (**M4-F05**). Its three changes are value-equal, but that is proven rather than measured, and the record says so. The other four uncovered selectors are ones this task did not touch.
5. **Tripwires: none fired.** No new value at a call site, no own panel, no minted token, no `@theme` edit, no `.as-edge-*` alpha, nothing below 1280 px, `typo-tokens.test.js` unmodified.
6. **Guards: three touched, all counter-checked.** Two broke and both read `border-radius: 6px` literally — resolved through `cssTokens.js` instead (M3's own correction), which gains them a second regression: the step disappearing from `@theme` now fails them. The third was **green and wrong**: it demanded `!important` for a reason the cascade refutes. Rewritten to the assurance; it now also catches what the old one was blind to — with `!important` gone, order decides. **Eleven defects reintroduced, red every time, clean tree green.** The counter-check found three defects in my own new guard (wrong rule matched, first instead of last definition, rule-wide instead of per-property `!important`) — all three by running it, none by reasoning about it.
7. **`!important`: three fell, and the measurement is why (M4-F04).** They carried nothing: `.go-actions > button` is both more specific than `.as-edge-strong` and later in the same unlayered sheet. Removal measured at **zero deltas over 25 189 nodes**. A fourth fell at `.go-card .go-box`, whose edge now goes through a redefinition exactly as M7's `.st-box` does. The round now stands at **ten fallen, two correctly kept**.
8. **Findings:** M4-F01 … M4-F09, in `measurements/M4.md`. The two that are the **owner's**, both counted and left untouched: **M4-F02** — three panel insets sit 1–3 px off the ladder (`15/17/17` vs `--in-base` 18; `12/15/14` vs `--in-snug` 13; `9/13` vs `--in-tight`/`--in-snug`); **M4-F03** — the unlock window's wash is `rgba(8, 8, 12, .82)`, 4/4/4 and +.02 alpha from `--sf-scrim`, visible at *every* width, where M3's comparable exception was granted at ≤ 1.8/255 and explicitly created no standing permission.
9. **M4-F01 corrects a prediction rather than reporting a result.** M8 wrote that pointing `.go-*` at `--sf-glass` would cost one delta entry per node, because the token spells `180deg` and the literal omits it. Measured: **zero** — the browser normalises the default angle out of the computed value. Fourth time this round that a document's observations held and its predictions did not.
10. **H-a answered by measurement:** TYPO-05's overflow is **147 nodes in all ten cells, both languages, both halves** — the recorded figure, unmoved.
11. **Gates, run bare without pipes:** `npm test` **exit 0** (143 files / **2296** tests) · `npm run lint -- --max-warnings=0` **exit 0** · `npm run build` **exit 0** · `npm run gen:db` **exit 0**. 2296 rather than 2292 because M4's units needed two ink and two edge ratchet entries; nothing was removed.
12. **Owner set:** `evidence/M4/owner/` — four PNGs, both languages at 1280×720 and 1920×1080. **One was opened and confirmed to be an image**, and that is what found **M4-F06**: my own harness reported `ok` while the first-start modal stood over the end screen, because clearing `localStorage` took `as_reset_epoch` with it. **A visual result is not reported as approved** — it is for the owner to look at.
13. **What the next task inherits.** (a) `evidence/M4/endscreen.mjs` — the census harness, and the one hook it still needs: a step that *places* an architect building, which would close M4-F05. (b) **M4-F07** — seeding a profile past onboarding does not reveal the screen's other branch; `onboarding` is a property of the RUN, not of the profile, so `.go-onb` needs an onboarding run played. (c) **M4-F08** — the de/1920×1080 `victory` cell is one node short of every other cell, pre-existing and in both halves. (d) `.go-blist`, `.go-onb`, `.go-unlocks` and `.ul-root` remain outside every machine cell in this workstream.
