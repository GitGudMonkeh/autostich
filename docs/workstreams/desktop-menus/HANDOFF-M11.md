# Handoff — M11 · the run dialogs · **the last task of the round**

1. **Done.** Migration only, as the contract requires. On `task/menu-m11-dialogs`, base `87680e3f`: `0e438a25` the two harnesses · `1501126b` the migration · `3dd26b8c` the guards and twenty counter-checks · `8c1675ff` record, evidence and owner set · this handoff. Tree clean, worktree left in place. **The task branch is pushed** — standing authorization in `AGENTS.md`, and the only way `ci.yml` runs at all. **No merge, no push of a permanent branch, no PR.**
2. **Did the vocabulary hold? Yes, to the last file — and the last file is the one that proves it cheaply.** Six values were steps and nothing was coined: both confirmations' wash **is** `--sf-scrim` (`rgba(12,12,16,.8)`, and `.8` is `0xcc` exactly), the loader's track and mask **are** `--sf-sunken`, and the dialog rows, the banner and its button read `--rd-sm` for their 6. The banner's `box-shadow: none` now reads **`--el-flat`** — §2c calls that step *"a step you pick, not an absence you have to remember"*, and that rule is where the absence was decided. **Scope was small at the end: the ladder was not straining, it was idle.**
3. **All sixteen surfaces at zero deltas, `hub` included.** 160 cells, 25 189 matched nodes, 0 unmatched, 0 not comparable, and the comparator's own last line: *"ZERO computed deltas on the four surface axes."* **That is the right gate here precisely because none of the sixteen is mine** — the survey cannot see these four, so its only job is to prove nothing else moved. `hub` matters most: `PwaInstall` is `StartScreen`'s, and that workstream has not begun.
4. **How each of the four was measured directly — the thing the survey cannot do.** `dialogs.mjs` and `banner.mjs`, both using a **scoped** copy of the gate's own probe (paths counted from the dialog, not from `body` — the confirmations stand over a live run, and a whole-document probe would drown four small dialogs in it). **16 cells / 80 matched nodes / 0 unmatched / zero deltas**, plus **4 / 12 / 0 / zero** for the banner, before the diff and again after. Every reach is written as *contains no X other than Y*: abort = one `.rc-wide` + three `.rc-row` and **no** `.rc-narrow`/`.rc-btn`; restart = its mirror; loader = one percentage readout **inside an overlay** with `.rn-shell` absent; install = one visible install button **and the hub behind it**; banner = one `.up-banner` **on the hub**.
5. **Two of the four cannot be reached at all without bending something, and both are correct product behaviour.** `RunLoader` (M11-F05): the hub paints the same 27 images the run start preloads — **27 set, 27 resolved, 0 network requests** — and clearing the HTTP cache changed nothing, because the app is a **PWA and its service worker was answering**, which the `Fetch` domain never sees. Bypassed: **65 requests**. `UpdateBanner` (M11-F01): it needs a CI build stamp a local `npm run build` does not set, so it is measured against a **stamped** build on its own port — same commit, same config, only the stamp differs.
6. **My own probe would have measured the running game (M11-F09).** The first loader predicate was *"exactly one childless node whose text is a percentage"* — and measured, the run stage carries **one of its own in German and two in English**. Ninth instance of that shape this round, and the **third** written by a worker holding the warning in its contract. Fixed by adding the negative half, like the other four.
7. **Three near misses, measured, none taken — and one of them the rule does not answer.** All read below 1280 px. `#1b1b24` vs `--sf-head` = **0/1/0** (M11-F02, the size the planner granted for M5-F02). `#2a2836` vs `--ed-quiet` = **0/2/2** (M11-F04, exactly on the line). And **`#0c0c10f2` vs `--sf-scrim-desk` = 0/0/0 in colour with the alpha alone moving 2.3/255** (M11-F03) — the threshold says *"≤ 2/255 per channel **and no alpha changes**"*, every precedent moved colour, two moved both, and **a pure-alpha case has never been decided.**
8. **Tripwires: none fired.** No new value at a call site, no own panel, no minted token, no `@theme` edit, no `.as-edge-*` alpha, no `--text-*`, nothing below 1280 px moved, no dead declaration off the five axes touched (the owner's ruling on M6), `typo-tokens.test.js` unmodified.
9. **Guards: measured which break. Three did, and none was predictable from a filename.** `rahmen-huelle.test.js` and `up-ruhe.test.js` went **red because the migration succeeded** — they spelled `6px` and `none` where the rules now name the step, so their own statements had become *more* true. `overlay-nesting.test.js` went red **because I wrote a comment**. `panel-tokens.test.js` was green only because these four were not yet on its allowlist.
10. **The two red-on-success guards were rewritten to the invariant, not to a second spelling.** Three parts per axis, and the third is the one a mere rewording forgets: the rule reads **exactly this rung** (its declared value, resolved through `@theme`, equals the resolved `var(--rd-sm)` / `var(--el-flat)`), **the rung still carries its value**, and **it is not the neighbour**. Without the negative probe the radius passes on `--rd-md` and the shadow on `--el-rest` — banner rounder than intended, glow back, green.
11. **The third was not weakened, and that is deliberate (M11-F06).** `overlay-nesting` decides a structural question from the **260 characters before the class literal**; three lines of comment put `overlayPortal(` ~330 away. The invariant was never violated, so **the source moved** and the comment now stands above the `return`. Widening the window is the wrong repair — a wider window finds *someone else's* `overlayPortal(`. The structural fix belongs to whoever owns that guard; reported with the case that reproduces it.
12. **The allowlist closes a seam M3 left open, which is a fitting last act.** M3 wrote `/\.up-(?!banner)/` because *"`.up-banner` belongs to no migrated screen"*. It belongs to one now. The two expressions must therefore **partition** the sheet — every `.up-` rule covered by exactly one — and that has its own counter-check. **It had to be repaired before it was worth anything:** its first version restated the two regexes inside the test instead of reading the real entries, and stayed **green while both sabotages passed**. A counter-check that tests a copy tests nothing.
13. **Twenty counter-checks, run and restored in a `finally`, 0 unexpected** (`evidence/M11/counter-checks.txt`): four clean baselines, twelve red-on-defect, **two green-on-work**, two liveness. The green pair is load-bearing — two findings above are conversions the planner may grant, and if a guard blocked them the grant would arrive with a red suite.
14. **Gates, run bare without pipes:** `npm test` **exit 0** (143 files / **2317** tests) · `npm run lint -- --max-warnings=0` **exit 0** · `npm run build` **exit 0** · `npm run gen:db` **exit 0**. 2317 rather than 2306 = ten ratchet entries plus the seam counter-check; three guards rewritten in place, none lost an assertion. **CI green** — run `32882892385` at `0ef15f87`, the only run this branch has and the one that reached a conclusion. Read the run for the branch TIP, and do not mistake a **cancelled** row for a verdict: the concurrency group cancels the previous run on every push, and `gh run watch` exits 0 on a cancelled run. This commit records that result and therefore has a run of its own; it changes one sentence of this file and nothing the run measures. Owner set: `evidence/M11/owner/`, twenty PNGs, five surfaces × two languages × two viewports, **one opened and confirmed to be an image**; the loader's and the banner's caveats stand beside them, not in a footnote. A visual result is not reported as approved.
15. **What the round's last task leaves behind** — Part 7 of the record has it in full; the four a successor cannot reconstruct: **(a)** M11-F03 is a **pure-alpha** sub-1280 case and the threshold rule has no answer for it — washes and scrims differ in opacity more often than in hue, so the next strand meets it again. **(b)** Two kinds of value are invisible to every ratchet this round built: a colour reaching a component as a **prop** (M6-F11) and one held as a **module constant** (M11-F08) — so the ink numbers mean *ink in style objects*, and reading them as *ink* is wrong by five values in these four files alone. **(c)** `RunLoader` and `UpdateBanner` **can never be survey cells** as the harness stands; putting them in the matrix needs two new controls in `viewport-survey.mjs`, not a new cell. **(d)** M11-F10: one flaky cell left **81 of 160 cells** carrying a different run count while the symptom showed on five — the matrix is a *sequence*, not a set, and the next sequence effect may land where nothing renders it at all.
