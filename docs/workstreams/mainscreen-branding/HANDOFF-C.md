# Handoff — C · the mainscreen · **the last unmigrated surface, and a design commission**

1. **Done, four commits, on `task/mainscreen-brand` from `2600c74f`:** `22219a99` C1 the measurement · `8099dc1a` the owner stop · `9ab1b6e9` C2 the head zone · `80a58b0e` C3 the deck panel · `bc4c377a` C4 the vocabulary. Tree clean, worktree left in place, branch pushed (standing authorization; **no merge, no permanent branch, no PR**). Records in `measurements/C1–C4.md`, evidence in `evidence/C1–C4/`. **Gates green at every commit, run bare without pipes** — the last: `npm test` **145 files / 2353 tests**, `lint --max-warnings=0`, `build`, `gen:db`, all exit 0. **CI green** — run `32911618842` at `bc4c377a`, the branch tip, the only run this branch has and the one that reached a conclusion. *(This commit records that result and therefore has a run of its own; it changes one file of prose and nothing that run measures.)*
2. **Yes — the head zone holds all three at 1280 × 720**, at the mark's full 5 × 8 size, in both languages, with page overflow unchanged at **0**. It costs the page nothing at any viewport. **Q9 is closed unasked** (planning report §8.2). The reason nobody had measured: `.hub-pair` centres, so the pair is as tall as its **taller** column — always the right one — and the left column has **267 px free** against a lockup that costs **123**.
3. **The same measurement moved the top risk to C3, and that was the one owner stop.** The right column tolerates **+7 px at 1280 × 720**. The design's 196 × 268 deck field adds ≥114 and overflowed at three of five viewports; three options were **built and priced** rather than described, and the owner chose **A — the art follows the viewport** (Q10). It now reaches 194 × 268 from 1600 px up, and **1400 × 700 stops scrolling**: the 14 px it carried before this workstream began were the deck art's.
4. **Did the vocabulary hold? Mostly, and the misses are counted rather than coined.** Four literals became steps where the step fits by **role and value** — `--sf-ground`, `--ed-quiet` (0/0/1), `--rd-md` twice (0 and 1 px). **Twelve deviations are in a named table with their measured distances** (`C4.md` §3.3): three panel insets 4–6 px off `--in-base`, one radius 2 px off `--rd-lg`, three values the ladder has no *kind* for (translucent surface, translucent edge, a game object's shadow), a state, a meaning-coded edge, a skin variant, and the phone's carriers. **Nothing was minted and no tripwire fired.**
5. **Machine half — and it is stronger than the gate asked for.** C4 against C3: **0 unmatched nodes**, and the only deltas are **the two migrations the record predicted before the run** — 440 × `9px → 8px` and 110 × `rgb(42,42,51) → rgb(42,42,52)`. Outside `.hub-root`: **zero, 160 cells, 20 239 nodes.** **`run-stage` is at zero in every one of the four comparisons**, with the four other in-run surfaces — the wordmark trap never fired.
6. **The gate's wording is too coarse for this screen, not too strict, and the record says so rather than reporting a green it does not have.** The hub stands behind every menu overlay, so C2's one added node shifted eleven surfaces' paths at once (2750 deltas, every one under `0/0/2/0/5/0`, measured to be `.hub-play` with the options overlay open). C3's 1400 × 700 shows 2624 more, **all four of them box coordinates and none paint**: the scrollbar is gone, so eleven screens got 8 px of width back.
7. **Below 1280 px: zero, at every commit** — six cells, three widths, both languages, everything compared. It took a rewrite to get there: the media-query version of the letter needs the glyph in its own inline element, and that moved the wordmark's width by **0.02 px** in two of six cells. Invisible, still a movement this round excludes. `useIsWide` — whose own comment names exactly this case — makes it zero.
8. **Inline alphas: two converted, two deliberately not.** `.25` (deck art) and `.22` (music box) left the JSX for **one rule each** — the chip row would have copied `.22` three times. `.10` and `.18` stay: they render below 1280 px, where an inline style has no media query. **The family here is six, not two** (C4-F02) — MH1 counted the JSX file's literals and the screen carries `.16` twice in the stylesheet as well. All six are in MENU-38's ratchet, each alpha asserted by name.
9. **Guards: two written, two rewritten, one hole found in my own list.** New: `marke.test.js` (18) and `deck-tafel.test.js` (13), every check as *"contains no X other than Y"*. `desktop-perf.test.js` went **red because a comment was written** — second time this round — and is rewritten to its invariant; the strengthening is **measured**, not claimed (CC12/CC12′: same defect masked by a comment, green before, red after). `panel-tokens.test.js` gained the mainscreen, both ratchets and twelve exemptions.
10. **The hole is worth the planner's attention (C4-F01).** An exemption names a **selector**, and a selector spans both halves of the stylesheet: my entry for `.as-hub-field`'s **phone** rule was silently covering its **desktop** rule. Found because CC1 and CC2 stayed green with the raw literals put back. Fixed here by scoping the desktop rules — and then measured across the whole guard: **16 of 187 exemption entries, from five workers, reach both halves.**
11. **Counter-checks: 36 sabotage runs across three sets, 8 baselines, 0 unexpected** (`evidence/C2–C4/counter-checks.txt`). Three of them were wrong before they were right, and all three are kept with the note: a sabotage in a comment (the guard strips comments, correctly), one aimed at a guard that was never asked, and one pairing a CSS defect with the removal of a JSX entry. **A counter-check that fails tells you which of the two is broken only if you go and look.**
12. **What the contract said and the tree does not carry.** `.hub-*`/`.as-hub-*` "19 and 31 rules" are **line counts with comments in them** (14/26 rules); "22 colour values" is **26 distinct, 28 occurrences**, nine of them outside any style object; the wordmark's recorded 98 px collision point is **96.4**; the `zoom` floor's stated reason lands on nothing measurable in the button it names; and **`dt:pl-6` was already dead**, beaten by a two-class rule since the list rebuild.
13. **One thing needs your eye and is not reported as approved: the letter at 1280 × 720.** Its geometry is verified against the design's table to the rounding of `offsetHeight`, and an unlit cell is a **0.9 device-px ring** at 22 % white. Whether that reads is a visual judgement. **Four alternatives are built and captured at true 1×** — `evidence/C2/owner/wordmark-1x-de-*.png` — so answering costs one look. What ships is the design as written.
14. **Reported, not built:** the design's four open points (deck brightness across 52 looks · the Genesis two-colour gradient · German names for three effects · where the tutorial offer goes) and **the deck tier row** — its data is computed in `App.jsx`, a named non-goal, so it is out for the same reason as Build DNA. Owner set: `evidence/C4/owner/`, both languages × two sizes × a **dark and a bright** deck, several opened and confirmed to be images.
15. **Two things the next reader needs.** **(a)** `origin/feature/desktop-menus` is **two commits ahead** of the base this contract named (MH3b and its merge); they touch the harness and no file of mine, and MH3b's change to `viewport-survey.mjs` is exit-code handling, not measurement — but the integration is a merge, not a fast-forward. **(b) The contract's five viewports cannot see a whole class of error**: all five sit exactly on the `zoom` clamp's floor, which only rises above 0.85 from 1632 px of width. Two versions of the deck-art formula were zero at all five and overflowed by up to 89 px outside them. The instrument now measures **eight**.
