  BASELINE before  exit 0  GREEN as required
  CC1   exit 1  RED as required  — Flaeche: a raw fill returns to a migrated rule
  CC2   exit 1  RED as required  — Kante: a raw edge returns
  CC3   exit 1  RED as required  — Radius: a raw radius returns to the attribute chip
  CC4   exit 1  RED as required  — Hoehe: an elevation literal appears where the ladder has a step
  CC5   exit 1  RED as required  — Innenabstand: a panel inset appears on a rule no exemption names
  CC6   exit 1  RED as required  — a desktop padding utility returns to the JSX — the fourth spelling
  CC7   exit 1  RED as required  — an arbitrary utility returns — the third spelling
  CC8   exit 1  RED as required  — an ink literal is added — the ratchet counts and does not coin
  CC9   exit 1  RED as required  — a seventh translucent edge is added to the unit that carries six
  CC10  exit 1  RED as required  — an exemption is pointed at a selector that no longer exists
  CC11  exit 1  RED as required  — the deck panel's own hook is removed — every C3 rule would then dress nothing
  CC12a exit 0  GREEN — the tree passes without the entry too  — the entry costs nothing on a clean tree, which is what an allowlist should do
  CC12b exit 0  GREEN without the entry — so the entry is what catches CC6  — CC6's defect, guard without the mainscreen entry
  BASELINE after   deck exit 0 GREEN
  BASELINE after   exit 0  GREEN — the tree is restored

  11 sabotages + 2 entry probes, 2 baselines, 0 unexpected
